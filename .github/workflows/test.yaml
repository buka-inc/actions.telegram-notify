name: Test

on: push

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - id: telegram
        uses: ./
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chartId: ${{ secrets.TELEGRAM_CHAT_ID }}
          messageThreadId: 1199
          text: "actions.telegram-notify 测试消息"
      - run: |
          [[ "${{ steps.telegram.outputs.success }}" == "true" ]] || exit 1
